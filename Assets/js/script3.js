// Coordonnées
function ecranCoordonnees() {
    el = document.getElementById("ecranCoordonnees");
    el.style.visibility = (el.style.visibility == "visible") ? "hidden" : "visible";
    }
// Formation
function ecranFormation() {
    el = document.getElementById("ecranFormation");
    el.style.visibility = (el.style.visibility == "visible") ? "hidden" : "visible";
    }
// Expérience
function ecranExperience() {
    el = document.getElementById("ecranExperience");
    el.style.visibility = (el.style.visibility == "visible") ? "hidden" : "visible";
    }