// Xtof
var xtof = getElementById('ecranXtof')
function ecranXtof() {
    xtof = document.getElementById('ecranXtof');
    xtof.style.visibility = (xtof.style.visibility == "visible") ? "hidden" : "visible";
    }
// Coordonnées
    var coordonnees = getElementById('ecranCoordonnees')
    function ecranCoordonnees() {
    coordonnees = document.getElementById("ecranCoordonnees");
    coordonnees.style.visibility = (coordonnees.style.visibility == "visible") ? "hidden" : "visible";
    }
// Formation
    var formation = getElementById('ecranFormation')
    function ecranFormation() {
    formation = document.getElementById("ecranFormation");
    formation.style.visibility = (formation.style.visibility == "visible") ? "hidden" : "visible";
    }
// Expérience
    var experience = getElementById('ecranExperience')
    function ecranExperience() {
    experience = document.getElementById("ecranExperience");
    experience.style.visibility = (experience.style.visibility == "visible") ? "hidden" : "visible";
    }
// Loisirs
    var loisirs = getElementById('loisirs')
    function ecranLoisirs() {
    loisirs = document.getElementById("ecranLoisirs");
    loisirs.style.visibility = (loisirs.style.visibility == "visible") ? "hidden" : "visible";
    }
// Competences
    var competences = getElementById('ecranCompetences')
    function ecranCompetences() {
    competences = document.getElementById('ecranCompetences');
    competences.style.visibility = (competences.style.visibility == "visible") ? "hidden" : "visible";
    }
// Informatique
    var informatique
    function ecranInformatique() {
    informatique = document.getElementById("ecranInformatique");
    informatique.style.visibility = (informatique.style.visibility == "visible") ? "hidden" : "visible";
    }

        // Competences
// function ecranCompetences() {
//     el = document.getElementById("ecranCompetences");
//     el.style.visibility = (el.style.visibility == "visible") ? "hidden" : "visible";
//     }